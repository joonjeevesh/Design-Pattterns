package org.example.structural.decorator;

public class StringUtils {
    public static String compress(String string) {
        //some complex compress logic or library func, returning string as it is for now
        return string;
    }

    public static String decompress(String string) {
        //some complex decompress logic or library func, returning string as it is for now
        return string + " - Decompressed";
    }

    public static String encrypt(String string) {
        //some complex encryption logic or library func, returning string as it is for now
        return string;
    }

    public static String decrypt(String string) {
        //some complex decryption logic or library func, returning string as it is for now
        return string + " - Decrypted";
    }
}
